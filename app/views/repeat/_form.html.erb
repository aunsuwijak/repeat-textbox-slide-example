<%= form_for(@repeat) do |f| %>
  <% if @repeat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repeat.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @repeat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>